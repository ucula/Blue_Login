import { ContainerHeader } from "./ContainerHeader";

export function AddUserHeader() {
  return (
    <ContainerHeader
      title="Add User"
      subtitle="Add a new team member to your dashboard."
    />
  );
}
